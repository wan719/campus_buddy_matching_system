<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.swu.campus_buddy_matching_system.mapper.RoleMapper">

    <!-- BaseResultMap for mapping role properties -->
    <resultMap id="BaseResultMap" type="cn.edu.swu.campus_buddy_matching_system.model.entity.Role">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- Select roles by user -->
    <select id="selectByUser" resultMap="BaseResultMap">
        SELECT DISTINCT
            r.id,
            r.name,
            r.description,
            r.created_at,
            r.updated_at
        FROM roles r
        LEFT JOIN user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
        ORDER BY r.id
    </select>

    <!-- Select role with permissions -->
    <select id="selectWithPermissions" resultMap="RoleWithPermissionsResultMap">
        SELECT
            r.id,
            r.name,
            r.description,
            r.created_at,
            r.updated_at,
            p.id AS permission_id,
            p.name AS permission_name,
            p.description AS permission_description,
            p.resource AS permission_resource,
            p.action AS permission_action,
            p.created_at AS permission_created_at,
            p.updated_at AS permission_updated_at
        FROM roles r
        LEFT JOIN role_permission rp ON r.id = rp.role_id
        LEFT JOIN permissions p ON rp.permission_id = p.id
        WHERE r.id = #{id}
        ORDER BY p.resource, p.action
    </select>

</mapper>