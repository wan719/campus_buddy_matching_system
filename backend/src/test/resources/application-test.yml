spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_buddy_matching_system_test?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 20060719li
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 30000

  sql:
    init:
      # Only initialize and reset schema in test profile / test database.
      mode: always
      schema-locations: classpath:db/schema.sql
      data-locations: classpath:db/data.sql
      continue-on-error: true

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.edu.swu.campus_buddy_matching_system.model.entity

jwt:
  secret: test-secret-key-for-unit-tests-2024
  expiration: 86400000

logging:
  level:
    cn.edu.swu: DEBUG
    org.springframework.jdbc: DEBUG
    org.mybatis: DEBUG
    org.springframework.boot.autoconfigure: WARN
